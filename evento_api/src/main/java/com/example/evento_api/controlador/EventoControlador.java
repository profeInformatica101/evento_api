package com.example.evento_api.controlador;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


@RestController
@RequestMapping("/api/v0/evento")
public class EventoControlador {
/*
 * Crear Evento (Solo para Managers)
POST /api/v0/eventos
Permite a los managers crear nuevos eventos.

Listar Todos los Eventos
GET /api/v0/eventos
Muestra todos los eventos disponibles, posiblemente con filtros por estado o tipo.

Obtener Detalles de un Evento
GET /api/v0/eventos/{idEvento}
Proporciona detalles de un evento específico.

Actualizar Evento (Solo para Managers)
PUT /api/v0/eventos/{idEvento}
Permite a los managers actualizar los detalles de un evento existente.

Eliminar Evento (Solo para Managers)
DELETE /api/v0/eventos/{idEvento}
Permite a los managers eliminar un evento.

 */
	
	/** INSCRIPCIONES A EVENTOS */
}
